<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Vols - TripToGo</title>
    <style>
        @page {
            margin: 0;
            size: landscape;
        }
        body {
            font-family: 'Helvetica', Arial, sans-serif;
            margin: 0;
            padding: 30px;
            color: #2c3e50;
            background: #ffffff;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding: 20px;
            background: #ff6b00;
            color: white;
            border-radius: 10px;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: bold;
            color: #fff;
            margin-bottom: 5px;
        }
        .header p {
            margin: 0;
            font-size: 14px;
            color: #fff;
        }
        .container {
            width: 100%;
            margin: 0 auto;
        }
        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin-top: 20px;
            font-size: 12px;
            background: white;
            border-radius: 10px;
            overflow: hidden;
        }
        th {
            background: #ff6b00;
            color: white;
            padding: 12px;
            text-align: left;
            font-weight: bold;
            font-size: 11px;
        }
        th:first-child {
            border-top-left-radius: 10px;
        }
        th:last-child {
            border-top-right-radius: 10px;
        }
        td {
            padding: 12px;
            border-bottom: 1px solid #e0e0e0;
            color: #333;
        }
        tr:last-child td {
            border-bottom: none;
        }
        tr:nth-child(even) {
            background-color: #f5f5f5;
        }
        .status {
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: bold;
            font-size: 11px;
            display: inline-block;
        }
        .status-available {
            background-color: #4caf50;
            color: white;
        }
        .status-unavailable {
            background-color: #f44336;
            color: white;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 11px;
            color: #666;
        }
        .price {
            font-weight: bold;
            color: #ff6b00;
        }
        .date {
            color: #ff6b00;
            font-weight: 500;
        }
        .flight-info {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .flight-route {
            font-weight: 500;
        }
        .flight-arrow {
            color: #ff6b00;
        }
        .class-badge {
            background-color: #fff3e0;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 11px;
            color: #ff6b00;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Liste des Vols</h1>
            <p>Généré le {{ "now"|date("d/m/Y H:i") }}</p>
                </div>

        <table>
                                        <thead>
                                        <tr>
                    <th>ID</th>
                    <th>Compagnie</th>
                    <th>Itinéraire</th>
                    <th>Horaires</th>
                    <th>Prix</th>
                    <th>Places</th>
                    <th>Classe</th>
                    <th>Statut</th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% for vol in vols %}
                                            <tr>
                        <td>#{{ vol.id }}</td>
                        <td>{{ vol.compagnie }}</td>
                                                <td>
                            <div class="flight-info">
                                <span class="flight-route">{{ vol.depart }}</span>
                                <span class="flight-arrow">→</span>
                                <span class="flight-route">{{ vol.destination }}</span>
                                                    </div>
                                                </td>
                                                <td>
                            <div class="flight-info">
                                <div>
                                    <div class="date">{{ vol.heureDepart|date('d/m/Y') }}</div>
                                    <div style="color: #666; font-size: 11px;">{{ vol.heureDepart|date('H:i') }} - {{ vol.heureArrivee|date('H:i') }}</div>
                                                        </div>
                                                    </div>
                                                </td>
                        <td class="price">{{ vol.prix }} DT</td>
                        <td>{{ vol.siegesDisponibles }}</td>
                        <td><span class="class-badge">{{ vol.classeChaise }}</span></td>
                        <td>
                            <span class="status {% if vol.siegesDisponibles > 0 %}status-available{% else %}status-unavailable{% endif %}">
                                {{ vol.siegesDisponibles > 0 ? 'Disponible' : 'Complet' }}
                            </span>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>

        <div class="footer">
            <p>© {{ "now"|date("Y") }} TripToGo - Système de Gestion des Vols</p>
        </div>
    </div>
</body>
</html>