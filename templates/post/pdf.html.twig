<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ post.type == 'announcement' ? post.announcementTitle : post.surveyQuestion }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #e78d1e;
            padding-bottom: 20px;
        }
        .title {
            color: #e78d1e;
            font-size: 24px;
            margin-bottom: 10px;
        }
        .meta {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }
        .content {
            margin-bottom: 30px;
        }
        .image-container {
            text-align: center;
            margin: 20px 0;
        }
        .image-container img {
            max-width: 100%;
            height: auto;
        }
        .tags {
            margin-top: 20px;
        }
        .tag {
            display: inline-block;
            background: #e78d1e;
            color: white;
            padding: 4px 12px;
            border-radius: 15px;
            margin-right: 8px;
            margin-bottom: 8px;
            font-size: 12px;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1 class="title">{{ post.type == 'announcement' ? post.announcementTitle : post.surveyQuestion }}</h1>
        <div class="meta">
            Posted on: {{ post.dateCreation|date('F j, Y') }}
            {% if post.dateModification and post.dateModification != post.dateCreation %}
                <br>Last modified: {{ post.dateModification|date('F j, Y') }}
            {% endif %}
        </div>
    </div>

    {% if post.cheminFichier %}
        <div class="image-container">
            <img src="{{ absolute_url(asset('uploads/' ~ post.cheminFichier)) }}" alt="Post image">
        </div>
    {% endif %}

    <div class="content">
        {% if post.type == 'announcement' %}
            <h2>Content</h2>
            <p>{{ post.announcementContent|nl2br }}</p>
            
            {% if post.announcementTags %}
                <div class="tags">
                    <h3>Tags</h3>
                    {% for tag in post.announcementTags|split(',') %}
                        <span class="tag">{{ tag|trim }}</span>
                    {% endfor %}
                </div>
            {% endif %}
        {% else %}
            <h2>Survey Question</h2>
            <p>{{ post.surveyQuestion|nl2br }}</p>
            
            {% if post.surveyTags %}
                <div class="tags">
                    <h3>Tags</h3>
                    {% for tag in post.surveyTags|split(',') %}
                        <span class="tag">{{ tag|trim }}</span>
                    {% endfor %}
                </div>
            {% endif %}
        {% endif %}
    </div>

    <div class="footer">
        <p>Generated by TripToGo on {{ "now"|date('F j, Y') }}</p>
    </div>
</body>
</html> 