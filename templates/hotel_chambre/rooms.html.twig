{% extends 'base.html.twig' %}

{% block title %}{{ hotel.nom }} - Chambres{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row mb-4">
            <div class="col-12">
                <h1 class="text-custom">{{ hotel.nom }}</h1>
                <p class="text-muted">{{ hotel.description }}</p>
            </div>
        </div>

        <div id="room-list-container">
            {% include 'hotel_chambre/_room_list.html.twig' %}
        </div>
    </div>
{% endblock %} 